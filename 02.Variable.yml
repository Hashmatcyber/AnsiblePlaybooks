# variable-demo.yml
---
- name: Variable Demo Playbook
  hosts: all
  vars:
    greeting: "My"
    target: "World"
  tasks:
    - name: Print greeting message using variables
      debug:
        msg: "{{ greeting }} {{ target }}!"
